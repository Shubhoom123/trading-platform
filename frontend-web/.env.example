# Copy to .env.local.
# Base URLs of the Go gateway (the trading front door) and the Python analytics
# service (real data + impact models).
VITE_API_BASE=http://localhost:8090
VITE_WS_BASE=ws://localhost:8090
VITE_SIM_BASE=http://localhost:8100
