# internal/pb — generated code

Do not edit by hand. `trading.pb.go` and `trading_grpc.pb.go` are generated
from `../../../proto/trading.proto` by `make proto` (see the Makefile).

They are intentionally not committed as source you edit; CI runs `make proto`
before building. If you build locally, run `make tools && make proto` once.
